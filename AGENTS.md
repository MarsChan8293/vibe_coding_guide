# 项目协作规则

## 修改后的验证与预览

每次修改代码后，按以下顺序验证：

1. 先运行项目完整测试：

   ```bash
   npm test
   ```

   如果修改只涉及某个局部，可以先运行针对性的测试，但交付前仍要运行完整测试。

2. 测试通过后启动本地开发服务器：

   ```bash
   npm run dev
   ```

3. 按改动类型决定是否人工浏览器检查：

   - 纯文案、链接、MDX 元数据等不影响页面结构、样式或交互的简单修改：不调用浏览器，完成测试和本地服务启动即可；
   - 涉及布局、样式、组件、交互、响应式行为、资源路径或页面结构的修改：在浏览器中打开相关页面，人工检查桌面端和相关移动端的布局、文字溢出、交互、控制台错误和仓库子路径资源。

只有实际查看过页面时，才能声称已完成视觉验证。

## 发布边界

用户没有明确说“提交 Pages”“发布”“部署”“推送到 main”或同义指令时，只做本地修改、测试和预览，不主动执行：

- `git push`；
- 创建或合并 Pull Request；
- 推送或合并到 `main`；
- 触发 GitHub Pages 部署。

如需提交本地 commit，也等待用户明确提出提交或保存版本的要求。完成本地验证后，报告结果和工作区状态，等待发布指令。
